{
  "name": "remote-browser",
  "version": "1.0.0",
  "description": "Screenshot-based remote browser with native mobile keyboard support",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install-browser": "npx playwright install chromium"
  },
  "dependencies": {
    "express": "^4.22.1",
    "playwright": "^1.44.0",
    "ws": "^8.17.0"
  }
}
